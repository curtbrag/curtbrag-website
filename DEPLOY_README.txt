DEPLOY_MERGED

This is your full site: Sep-4 rollback + overlay fixes.

Options to publish:
1) Drag-and-drop: open Netlify -> Site -> Deploys -> 'Upload deploy', drop the 'deploy-merged' folder or the ZIP.
   If the site is locked to a deploy, unlock in the UI first (Deploys -> Unlock).
2) Git-connected: copy the contents of 'deploy-merged' into your repo at the same paths, then commit and push.

After publishing, verify the site, then revoke any temporary tokens you used.
