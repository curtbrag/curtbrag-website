# Minimal Netlify config
[build]
  publish = "."
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"

# Enable branch deploys for all branches
[context.branch-deploy]
  publish = "."

[context."claude/restore-sept-design-brug3"]
  publish = "."